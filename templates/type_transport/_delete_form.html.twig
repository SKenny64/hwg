<form method="post" action="{{ path('app_type_transport_delete', {'id': type_transport.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_transport.id) }}">
    <button class="btn">Delete</button>
</form>
