{% extends 'base.html.twig' %}

{% block title %}TypeTransport{% endblock %}

{% block body %}
    <h1>TypeTransport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_transport.id }}</td>
            </tr>
            <tr>
                <th>Name_transport</th>
                <td>{{ type_transport.nameTransport }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_transport_index') }}">back to list</a>

    <a href="{{ path('app_type_transport_edit', {'id': type_transport.id}) }}">edit</a>

    {{ include('type_transport/_delete_form.html.twig') }}
{% endblock %}
