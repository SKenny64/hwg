<form method="post" action="{{ path('app_coordonnees_organisateur_delete', {'id': coordonnees_organisateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coordonnees_organisateur.id) }}">
    <button class="btn">Delete</button>
</form>
