{% extends 'base.html.twig' %}

{% block title %}CoordonneesOrganisateur{% endblock %}

{% block body %}
    <h1>CoordonneesOrganisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coordonnees_organisateur.id }}</td>
            </tr>
            <tr>
                <th>RaisonSociale</th>
                <td>{{ coordonnees_organisateur.RaisonSociale }}</td>
            </tr>
            <tr>
                <th>SiteWeb</th>
                <td>{{ coordonnees_organisateur.SiteWeb }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ coordonnees_organisateur.telephone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_coordonnees_organisateur_index') }}">back to list</a>

    <a href="{{ path('app_coordonnees_organisateur_edit', {'id': coordonnees_organisateur.id}) }}">edit</a>

    {{ include('coordonnees_organisateur/_delete_form.html.twig') }}
{% endblock %}
